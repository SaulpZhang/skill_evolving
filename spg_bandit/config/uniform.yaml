embedding_model: all-MiniLM-L6-v2
embedding_type: local
max_turns: 50

warmup:
  split: valid_seen
  n_tasks: 60
  steps: 0

evolve:
  split: valid_seen

evaluate:
  split: valid_unseen

selector: uniform

skill_evolving:
  name: simple_agent
