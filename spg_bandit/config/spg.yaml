embedding_model: all-MiniLM-L6-v2
embedding_type: local
max_turns: 50

warmup:
  split: valid_seen

evolve:
  split: valid_seen

evaluate:
  split: valid_unseen

selector: spg_bandit

skill_evolving:
  name: simple_agent

spg_bandit:
  warmup_ratio: 0.3
  K: 4
  alpha: 0.1
  d_f: 16
  tau: 0.1
